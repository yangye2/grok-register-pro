# grok-register-pro

Grok ??????Pro???? SQLite ?????????SSO/Auth/CPA ?????? Grok2API / CPA / Sub2API ????

?????

- ?? API + Web ???
- SQLite ??????????????
- Camoufox ????????? bake ? `/opt/browser-cache`?
- ???? Turnstile Solver???? `127.0.0.1:5072`?????????

---

## ??????? Docker?

```bash
cp .env.example .env
# ?? .env?????????????????

mkdir -p ./data/register_pro
docker compose up -d --build
docker compose logs -f
```

??????

```text
http://127.0.0.1:8788/admin/accounts
```

???????????????? `.env` ????

```bash
GROK_REGISTER_ADMIN_BOOTSTRAP_PASSWORD='?????'
```

### ????

```bash
# GHCR
docker pull ghcr.io/yangye2/grok-register-pro:latest

# Docker Hub??????
docker pull yangye2/grok-register-pro:latest
```

??????????? `.env` ????

```bash
GROK_REGISTER_IMAGE=ghcr.io/yangye2/grok-register-pro:latest
docker compose up -d
```

---

## ?????????

?????**?????**???????????

?????

```text
./data/register_pro  ?  ?? /data
```

???????????????????

```text
data/register_pro/
  register_pro.sqlite3   # ????????
  backups/
  outputs/               # Auth / CPA ??
  cache/                 # ???????
  register_sso/
```

?????????

```bash
GROK_REGISTER_HOST_DATA_DIR=/var/lib/grok-register-pro
```

> ??????????????????????????

---

## ????????

????? `.env.example`?

| ?? | ?? | ?? |
|------|------|------|
| `GROK_REGISTER_PRO_PORT` | ??????? | `8788` |
| `GROK_REGISTER_HOST_DATA_DIR` | ????????? | `./data/register_pro` |
| `GROK_REGISTER_ADMIN_BOOTSTRAP_PASSWORD` | ??????? | ???? Web ??? |
| `GROK_REGISTER_ADMIN_BASE_PATH` | ??????? | `/admin` |
| `GROK2API_XAI_PROXY_POOL` | ????????/??? | ? |
| `GROK2API_MOEMAIL_*` | MoeMail ????????????? | ? |
| `TURNSTILE_THREAD` | ??????? | `1` |
| `TURNSTILE_LAZY` / `TURNSTILE_IDLE_SEC` | Docker ?? `0` / `0`????? | `0` / `0` |

---

## ????

- ?????????? + ?? Turnstile + SSO ? OAuth
- ???MoeMail / YYDS / GPTMail / CFMail / DuckMail / **Outmail**
- ??????????????
- ???SSO / Grok2API Auth ZIP / CPA ZIP
- ???Grok2API?CPA?`/v0/management/auth-files`??Sub2API
- ???????`cpa_pushed` / `sub2_pushed` / `grok2api_pushed`

---

## ?? / ???

- ???????? `camoufox fetch` ? `/opt/browser-cache`????????????????????
- ?? `shm_size: 1gb` ? `seccomp:unconfined`?compose ????
- ?????? `127.0.0.1:5072`????????
- Docker ?????????`TURNSTILE_LAZY=0`?`TURNSTILE_IDLE_SEC=0`

?????

```bash
# ?? API
curl -fsS http://127.0.0.1:8788/admin/api/session

# ???????
docker compose exec register-pro curl -fsS http://127.0.0.1:5072/health
```

---

## ????

```bash
docker compose up -d --build
docker compose logs -f
docker compose restart
docker compose down
docker compose exec register-pro bash
```

---

## GitHub Actions ??

????`.github/workflows/docker-image.yml`

**???**

- push ? `main` / `master`
- tag?`v*`?? `v1.2.25`?
- Actions ?? **Build Docker Image**

**???? GHCR?**

```text
ghcr.io/yangye2/grok-register-pro:latest
ghcr.io/yangye2/grok-register-pro:v1.2.25
```

**?? Docker Hub**??? Secrets??

- `DOCKERHUB_TOKEN`?????? `yangye2`?
- `DOCKERHUB_USERNAME` / `DOCKERHUB_IMAGE`??????

???????

```bash
docker build -t yangye2/grok-register-pro:latest .
docker push yangye2/grok-register-pro:latest

# ?
./scripts/publish_dockerhub.sh
```

??????

1. ??? `.env`?????`data/`?`generated/` ??????? `.dockerignore` ???
2. ??? Dockerfile / compose ?????????
3. ??????????????

---

## ?????Linux / macOS?

> Windows ???????????? `fcntl`?? `start.sh` ??????????Windows ?? Docker?

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
pip install -r turnstile-solver/requirements.txt

# ????
./turnstile-solver/start.sh

# ?????
./start.sh
```

- ????`http://127.0.0.1:8788/admin/accounts`
- ?????`./data/register_pro/`

---

## ????????

```text
???????/??/??
  ? ??????
  ? ?????Turnstile + ??????
  ? SSO ? OAuth ? Auth / CPA
  ? ?? SQLite
  ? ??????? Grok2API / CPA / Sub2API
```

### ??????

1. ?? healthy?`curl -fsS http://127.0.0.1:8788/admin/api/session`
2. ???????? `curl -fsS http://127.0.0.1:5072/health`
3. ??????? API / ??
4. ???? `GROK2API_XAI_PROXY_POOL`???????
5. ???????

---

## ????????

```text
register_pro_app.py      # FastAPI ?? API
register_pro_store.py    # SQLite ?? / ??
register_pro_config.py    # ???????
grok_build_adapter.py    # ??????
moemail.py / outmail_client.py
cpa_to_sub2api.py
turnstile-solver/        # ????
static/admin/            # ?????
Dockerfile / docker-compose.yml
```
